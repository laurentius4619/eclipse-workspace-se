
public class Encapsulation {
	/* OOP 의 방법 
	 *   1. 캡슐화
	 *   2. 상속성
	 *   3. 다형성(오버로딩,오바라이딩,객체형변환)
	 */

	/*
	 * 1. 캡슐화
	 *    - 외부클래스(객체)에서 멤버변수(필드)에 접근을 막고
	 *      외부클래스(객체)에서 멤버 메쏘드의 접근을 허용한다.
	 *    - 구현 : 맴버변수의 접근제한자를 private
	 *            맴버변수의 접근제한자는 public 으로한다. 
	 *             public ==> 어떤 외부클래스(객체)에서든지 접근가능
	 *             private==> 어떤 외부클래스(객체)에서든지 접근불가능
	 */
}
